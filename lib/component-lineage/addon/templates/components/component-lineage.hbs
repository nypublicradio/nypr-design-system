{{#if this.parents}}
  <p>
    <strong><code>{{@prefix}}/{{@name}}</code></strong> is used in
    {{#each this.parents as |parent|}}
      {{link-to parent.label parent.value}}
    {{/each}}
  </p>
{{/if}}

{{#if this.children}}
  <p>
    <strong><code>{{@prefix}}/{{@name}}</code></strong> uses
    {{#each this.children as |child|}}
      {{link-to child.label child.value}}
    {{/each}}
  </p>
{{/if}}
