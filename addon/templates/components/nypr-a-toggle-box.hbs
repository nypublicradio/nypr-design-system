{{!-- BEGIN-SNIPPET nypr-a-toggle-box.hbs --}}
{{#basic-dropdown
  calculatePosition=@calculatePosition
  horizontalPosition=@horizontalPosition
  verticalPostion=@verticalPostion
  renderInPlace=@renderInPlace
  onOpen=(action (mut this.open) true)
  onClose=(action (mut this.open) false)
  as |dd|}}
  {{yield (hash
    trigger=(component 'nypr-a-toggle-box/trigger' button=dd.trigger icon=icon)
    dropdown=(component dd.content class="c-toggle-box__dropdown")
  )}}
{{/basic-dropdown}}
{{!-- END-SNIPPET --}}
