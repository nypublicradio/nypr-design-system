import Component from '@ember/component';
import layout from '../../templates/components/nypr-o-article-header/lead';

export default Component.extend({
  layout,
  classNames: ['c-article__lead'],
});
