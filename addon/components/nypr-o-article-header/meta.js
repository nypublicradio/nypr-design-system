import Component from '@ember/component';
import layout from '../../templates/components/nypr-o-article-header/meta';

export default Component.extend({
  layout,
  classNames: ['l-container', 'l-container--10col'],
});
