import Component from '@ember/component';
import layout from '../../templates/components/nypr-o-featured-block-list/heading';

export default Component.extend({
  layout,
  classNames: ['c-featured-blocks__heading'],
  tagName: 'h2',
});
