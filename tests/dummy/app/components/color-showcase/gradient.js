import Component from '@ember/component';
import layout from '../../templates/components/color-showcase/gradient';

export default Component.extend({
  layout,

  classNames: ['flex', 'justify-between']
});
