import Controller from '@ember/controller';

import {
  DEFAULT_LEGAL,
  DEFAULT_SUBSCRIBED_MESSAGE,
  DEFAULT_LABEL
} from 'nypr-design-system/components/nypr-m-newsletter-form';


export default Controller.extend({
  DEFAULT_LABEL,
  DEFAULT_LEGAL,
  DEFAULT_SUBSCRIBED_MESSAGE,
});
